services:
  tracker:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - tracker-data:/app/data
    restart: unless-stopped
    environment:
      - FLASK_DEBUG=0

volumes:
  tracker-data: