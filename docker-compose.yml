services:
  tracker:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - tracker-data:/app/data
    env_file:
      - .env
    restart: unless-stopped


volumes:
  tracker-data: